# MTG Deck Lab (Flask) - runtime dependencies (Python 3.12+)
Flask>=3.0
requests>=2.31
beautifulsoup4>=4.12
Pillow>=10.2
# Optional OCR for the camera page (needs the Tesseract binary installed):
# pytesseract>=0.3.10
